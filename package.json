{
  "name": "workspace",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node index.js",
    "start": "node index.js",
    "build": "npm ci --include=dev && npm run build:server && npm run build:bot",
    "build:server": "cd server && npm ci --include=dev && npm run build",
    "build:bot": "cd bot && npm ci --include=dev && npm run build",
    "start-temp": "node scripts/auto-tunnel.js",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "concurrently": "^9.2.1",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "prisma": "^6.18.0"
  }
}
