# 🚀 Руководство по развертыванию Tip-Top на Replit

## 📋 Быстрый старт

Платформа Tip-Top теперь использует **встроенную PostgreSQL базу данных Replit** - ничего дополнительно настраивать не нужно!

### Шаг 1: PostgreSQL база данных ✅

**Уже готова!** PostgreSQL автоматически создана в Replit при инициализации проекта.

- ✅ DATABASE_URL уже настроен
- ✅ Миграции применены
- ✅ Таблицы созданы

Ничего делать не нужно!

### Шаг 2: Настройка Telegram Бота (2 минуты)

1. **Откройте Telegram** и найдите бота **@BotFather**
2. **Отправьте команду** `/newbot`
3. **Введите имя** вашего бота (например: `Tip-Top Games`)
4. **Введите username** (должен заканчиваться на `bot`, например: `tiptop_games_bot`)
5. **Скопируйте токен** который вам выдаст BotFather (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
6. **Узнайте ваш Telegram ID**:
   - Откройте бота **@userinfobot** в Telegram
   - Отправьте любое сообщение
   - Скопируйте ваш ID (это число, например: `123456789`)

### Шаг 3: Добавление секретов в Replit (1 минута)

1. **Откройте панель Secrets в Replit**:
   - Нажмите на иконку 🔒 (Secrets) в левой панели Tools
   - Или нажмите Ctrl+K и найдите "Secrets"

2. **Добавьте следующие секреты** (нажмите "Add new secret" для каждого):

   ```
   BOT_TOKEN = ваш_токен_от_BotFather
   BOT_USERNAME = @ваш_бот_username
   ADMIN_ID = ваш_telegram_id
   ```

   **Пример:**
   ```
   BOT_TOKEN = 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   BOT_USERNAME = @tiptop_games_bot
   ADMIN_ID = 123456789
   ```

   **ВАЖНО:** `DATABASE_URL` уже есть (встроенная PostgreSQL), `CLIENT_URL` будет установлен автоматически!

### Шаг 4: Запуск платформы 🚀

**ВАЖНО: Development vs Production режимы**

Платформа Tip-Top может работать в двух режимах:

#### 🔧 Development режим (для разработки на Replit)

При нажатии **"Run"** запускаются только **Server (API + Socket.IO)** и **Telegram Bot**.  
Это сделано для экономии ресурсов Replit.

**Что запускается автоматически:**
- ✅ Express API сервер (порт 5000)
- ✅ PostgreSQL база данных (встроенная)
- ✅ Socket.IO для real-time чата
- ✅ Telegram бот

**Что НЕ запускается (требует отдельных терминалов):**
- ⚠️ React Client (UI для пользователей)
- ⚠️ React Admin (панель администратора)

Для ПОЛНОГО dev окружения откройте дополнительные терминалы:
```bash
# Терминал 2 - Клиент
cd client && npm install && npm run dev

# Терминал 3 - Админ
cd admin && npm install && npm run dev
```

#### 🚀 Production режим (для публикации)

При публикации (Deploy) сервер автоматически отдает собранные React приложения как статику:
- ✅ Client доступен на основном URL: `https://ваш-домен.repl.co/`
- ✅ Admin доступен на: `https://ваш-домен.repl.co/admin`
- ✅ API доступен на: `https://ваш-домен.repl.co/api/*`
- ✅ PostgreSQL работает автоматически

Все 4 компонента работают через один порт!

---

**Запуск в Development:**

После добавления всех секретов:

1. **Нажмите кнопку "Run"** в верхней части Replit
2. **Подождите** пока установятся все зависимости (2-3 минуты при первом запуске)
3. **Проверьте логи** - вы должны увидеть:
   ```
   ✅ Все переменные окружения настроены!
   🚀 Запуск платформы Tip-Top...
   ✅ PostgreSQL connected successfully via Prisma
   ✅ Telegram бот запущен
   ```

### Шаг 5: Публикация и настройка домена 🌐

1. **Нажмите кнопку "Deploy"** в верхней части Replit
2. **Выберите тип деплоя**: Autoscale (рекомендуется)
3. **Дождитесь завершения** деплоя
4. **Скопируйте URL** вашего приложения (будет вида: `https://tip-top.username.repl.co`)
5. **Обновите секрет CLIENT_URL** (опционально):
   - Откройте Secrets
   - Добавьте `CLIENT_URL` = ваш URL деплоя (если не установлен автоматически)
6. **Перезапустите деплой**

### Опционально: Свой домен

Если у вас есть свой домен:

1. Перейдите в **Deployments** → **Settings** → **Link a domain**
2. Добавьте ваш домен
3. Настройте DNS записи как указано в Replit
4. Обновите `CLIENT_URL` в Secrets на ваш новый домен

---

## 🗄️ База данных

### PostgreSQL (встроенная в Replit)

Платформа использует **встроенную PostgreSQL базу данных Replit** с **Prisma ORM**.

**Преимущества:**
- ✅ Автоматическая настройка
- ✅ Бесплатная для разработки
- ✅ Типобезопасные запросы через Prisma
- ✅ Миграции через Prisma
- ✅ Поддержка транзакций

**Структура базы:**
- `users` - пользователи платформы
- `games` - каталог игр
- `offers` - предложения в играх
- `orders` - заказы пользователей
- `order_details` - детали заказов
- `payments` - платежи (USDT/RUB)
- `chats` + `chat_messages` - система чата
- `reviews` - отзывы пользователей
- `referrals` - реферальная система
- `transactions` - история транзакций
- `withdrawals` - выводы средств

### Миграции

Все миграции уже применены при первом запуске!

Если нужно внести изменения в схему:

```bash
cd server
npx prisma migrate dev --name название_миграции
```

### Просмотр данных

Используйте Prisma Studio:

```bash
cd server
npx prisma studio
```

Откроется веб-интерфейс для просмотра и редактирования данных.

---

## 🔧 Решение проблем

### Проблема: DATABASE_URL не найден

**Решение:**
PostgreSQL база должна быть создана автоматически. Если нет:
1. Нажмите "Database" в левой панели Replit
2. Выберите "PostgreSQL"
3. DATABASE_URL добавится в Secrets автоматически

### Проблема: Ошибка миграций Prisma

**Решение:**
```bash
cd server
npx prisma migrate reset
npx prisma migrate dev --name init
```

### Проблема: Telegram бот не отвечает

**Решение:**
1. Проверьте правильность BOT_TOKEN в Secrets
2. Убедитесь что BOT_USERNAME начинается с @
3. Проверьте логи на наличие ошибок подключения

### Проблема: Socket.IO не работает

**Решение:**
1. Проверьте что CLIENT_URL правильно настроен
2. Проверьте CORS настройки в server/index.ts
3. Убедитесь что сервер запущен на порту 5000

---

## 📊 Мониторинг

### Логи в Development

Все компоненты выводят логи в консоль:
- 🟢 Server: логи Express + Prisma
- 🔵 Bot: логи Telegram бота
- 🟡 Client: логи React dev server
- 🟠 Admin: логи React dev server

### Логи в Production

После деплоя логи доступны в разделе **Deployments** → **Logs**

---

## 🔐 Безопасность

1. **Никогда не коммитьте секреты** в Git
2. Используйте **Replit Secrets** для всех чувствительных данных
3. PostgreSQL DATABASE_URL автоматически защищен
4. Все пароли хешируются через bcryptjs
5. JWT токены для админ-панели

---

## 📦 Структура проекта

```
tip-top/
├── server/          # Express API + Socket.IO
│   ├── prisma/      # Prisma схема и миграции
│   ├── src/
│   │   ├── db/      # Prisma repositories
│   │   ├── routes/  # API роуты
│   │   └── controllers/ # Бизнес-логика
├── bot/             # Telegram бот (Telegraf)
├── client/          # React клиент (Telegram Web App)
├── admin/           # React админ-панель
├── index.js         # Главный launcher
└── DEPLOYMENT_GUIDE.md
```

---

## 💡 Полезные команды

```bash
# Запуск в dev режиме
npm run dev

# Сборка для production
npm run build

# Запуск в production
npm start

# Prisma миграции
cd server && npx prisma migrate dev

# Prisma Studio (просмотр данных)
cd server && npx prisma studio

# Генерация Prisma Client
cd server && npx prisma generate
```

---

## 🎉 Готово!

После выполнения всех шагов платформа Tip-Top полностью развернута и работает!

**Что дальше:**
1. Настройте платежную систему (CryptoPay для USDT)
2. Добавьте игры в каталог через админ-панель
3. Настройте реферальную программу
4. Протестируйте все функции

Удачи! 🚀
