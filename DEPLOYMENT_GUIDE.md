# 🚀 Руководство по развертыванию Tip-Top на Replit

## 📋 Быстрый старт

### Шаг 1: Настройка MongoDB Atlas (бесплатно, 5 минут)

1. **Перейдите на** https://www.mongodb.com/atlas
2. **Нажмите "Try Free"** и зарегистрируйтесь (email + пароль)
3. **Создайте бесплатный кластер M0**:
   - Выберите AWS или Google Cloud
   - Регион с пометкой "Free Tier Available" (рекомендуется ближайший к России)
   - Имя кластера: `TipTopCluster`
4. **Создайте пользователя базы данных**:
   - Username: `tiptop`
   - Password: создайте надежный пароль (сохраните его!)
5. **Настройте доступ**:
   - Network Access → Add IP Address
   - Добавьте `0.0.0.0/0` (разрешить со всех IP)
6. **Получите строку подключения**:
   - Нажмите "Connect" на вашем кластере
   - Выберите "Drivers" → Node.js
   - Скопируйте строку подключения (она выглядит так):
     ```
     mongodb+srv://tiptop:<password>@tiptopcluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
     ```
   - Замените `<password>` на ваш реальный пароль

### Шаг 2: Настройка Telegram Бота (2 минуты)

1. **Откройте Telegram** и найдите бота **@BotFather**
2. **Отправьте команду** `/newbot`
3. **Введите имя** вашего бота (например: `Tip-Top Games`)
4. **Введите username** (должен заканчиваться на `bot`, например: `tiptop_games_bot`)
5. **Скопируйте токен** который вам выдаст BotFather (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
6. **Узнайте ваш Telegram ID**:
   - Откройте бота **@userinfobot** в Telegram
   - Отправьте любое сообщение
   - Скопируйте ваш ID (это число, например: `123456789`)

### Шаг 3: Добавление секретов в Replit (1 минута)

1. **Откройте панель Secrets в Replit**:
   - Нажмите на иконку 🔒 (Secrets) в левой панели Tools
   - Или нажмите Ctrl+K и найдите "Secrets"

2. **Добавьте следующие секреты** (нажмите "Add new secret" для каждого):

   ```
   BOT_TOKEN = ваш_токен_от_BotFather
   BOT_USERNAME = @ваш_бот_username
   ADMIN_ID = ваш_telegram_id
   MONGODB_URI = mongodb+srv://tiptop:пароль@tiptopcluster...
   CLIENT_URL = (оставьте пустым, заполним после деплоя)
   ```

   **Пример:**
   ```
   BOT_TOKEN = 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   BOT_USERNAME = @tiptop_games_bot
   ADMIN_ID = 123456789
   MONGODB_URI = mongodb+srv://tiptop:myPassword123@tiptopcluster.ab1cd.mongodb.net/?retryWrites=true&w=majority
   CLIENT_URL = (пустое поле)
   ```

### Шаг 4: Запуск платформы 🚀

**ВАЖНО: Development vs Production режимы**

Платформа Tip-Top может работать в двух режимах:

#### 🔧 Development режим (для разработки на Replit)

При нажатии **"Run"** запускаются только **Server (API + Socket.IO)** и **Telegram Bot**.  
Это сделано для экономии ресурсов Replit.

**Что запускается автоматически:**
- ✅ Express API сервер (порт 5000)
- ✅ Socket.IO для real-time чата
- ✅ Telegram бот

**Что НЕ запускается (требует отдельных терминалов):**
- ⚠️ React Client (UI для пользователей)
- ⚠️ React Admin (панель администратора)

Для ПОЛНОГО dev окружения откройте дополнительные терминалы:
```bash
# Терминал 2 - Клиент
cd client && npm install && npm run dev

# Терминал 3 - Админ
cd admin && npm install && npm run dev
```

#### 🚀 Production режим (для публикации)

При публикации (Deploy) сервер автоматически отдает собранные React приложения как статику:
- ✅ Client доступен на основном URL: `https://ваш-домен.repl.co/`
- ✅ Admin доступен на: `https://ваш-домен.repl.co/admin`
- ✅ API доступен на: `https://ваш-домен.repl.co/api/*`

Все 4 компонента работают через один порт!

---

**Запуск в Development:**

После добавления всех секретов:

1. **Нажмите кнопку "Run"** в верхней части Replit
2. **Подождите** пока установятся все зависимости (2-3 минуты при первом запуске)
3. **Проверьте логи** - вы должны увидеть:
   ```
   ✅ Все переменные окружения настроены!
   🚀 Запуск платформы Tip-Top...
   ✅ MongoDB connected successfully
   ✅ Telegram бот запущен
   ```

### Шаг 5: Публикация и настройка домена 🌐

1. **Нажмите кнопку "Deploy"** в верхней части Replit
2. **Выберите тип деплоя**: Autoscale (рекомендуется)
3. **Дождитесь завершения** деплоя
4. **Скопируйте URL** вашего приложения (будет вида: `https://tip-top.username.repl.co`)
5. **Обновите секрет CLIENT_URL**:
   - Откройте Secrets
   - Добавьте/обновите `CLIENT_URL` = ваш URL деплоя
6. **Перезапустите деплой**

### Опционально: Свой домен

Если у вас есть свой домен:

1. Перейдите в **Deployments** → **Settings** → **Link a domain**
2. Введите ваш домен
3. Добавьте DNS записи в настройки вашего регистратора домена
4. Обновите `CLIENT_URL` на ваш кастомный домен

## 📦 Компоненты платформы

- **Server** (`/server`) - Express.js API сервер с Socket.IO для чата
- **Client** (`/client`) - React приложение для пользователей
- **Bot** (`/bot`) - Telegram бот для уведомлений и взаимодействия
- **Admin** (`/admin`) - React админ-панель для управления заказами

## 🔧 Полезные команды

```bash
# Проверка переменных окружения
bash check-env.sh

# Запуск в dev режиме
bash start-dev.sh

# Сборка для production
bash build-all.sh

# Запуск в production
bash start-prod.sh
```

## 📊 Порты

- **3000** - API сервер (Express + Socket.IO)
- **5173** - Клиент (dev режим)
- **5174** - Админ-панель (dev режим)

В production режиме клиент и админка отдаются как статика из сервера на порту 3000.

## ❓ Частые проблемы

### Проблема: "MongoDB connection error"
**Решение:**
- Проверьте что `MONGODB_URI` правильно скопирован
- Убедитесь что вы заменили `<password>` на реальный пароль
- Проверьте что добавили `0.0.0.0/0` в Network Access в MongoDB Atlas

### Проблема: "BOT_TOKEN не установлена"
**Решение:**
- Добавьте секрет через панель Secrets (🔒) в Replit
- НЕ редактируйте файлы .env вручную - используйте только Secrets

### Проблема: Бот не отвечает
**Решение:**
- Проверьте что `BOT_TOKEN` правильный
- Убедитесь что `BOT_USERNAME` указан с собакой (@)
- Проверьте логи бота в консоли Replit

## 🎉 Готово!

После выполнения всех шагов ваша платформа Tip-Top будет полностью развернута и готова к использованию!

Для управления:
- Пользователи → Telegram бот + веб-клиент
- Администраторы → Админ-панель по адресу `ваш_домен/admin`
